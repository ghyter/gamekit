{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "go build -o ${workspaceFolder}/cmd/gamekit ${workspaceFolder}/cmd/gamekit.go",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
